rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open read/write access for preview sandbox environment
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
